# v0.3-mvp-complete

Date: 2026-03-01

## Scope livré

- Auth multi-rôles et dashboards dédiés (`client`, `club`, `promoter`, `female_vip`, `admin`).
- Parcours réservation public avec checkout Stripe Elements et webhook idempotent.
- Module promoteur complet:
  - Guest list (ajout invité, statuts, arrivée optimiste, compteurs live).
  - Tracking promo/cookie, attribution réservation, commissions.
- Module club promoteurs:
  - Création promoteur (code promo unique, taux commission, email bienvenue).
  - Validation commissions pending et top performers.
- Dashboard client complet:
  - Home score + prochaine réservation,
  - Réservations (annulation conditionnelle / revente placeholder),
  - Waitlist (sortie en un clic).
- Landing page publique finalisée (dark luxury, SEO metadata, next/image).
- Seed démo exécutable (`npm run seed:demo`).

## Validation

- `npm run lint` ✅
- `npm run build` ✅
- `npm run seed:demo` ✅ (si variables Supabase service role disponibles)

## Commandes tag

```bash
git add .
git commit -m "feat: finaliser MVP v0.3 avec modules promoteur et dashboard client"
git tag -a v0.3-mvp-complete -m "NightTable MVP v0.3 complete"
git push origin master
git push origin v0.3-mvp-complete
```

## Comptes de démo seedés

- `demo.club@nighttable.app`
- `demo.promoter1@nighttable.app`
- `demo.promoter2@nighttable.app`
- `demo.client1@nighttable.app`
- `demo.client2@nighttable.app`
- `demo.vip@nighttable.app`

Mot de passe commun: `NighttableDemo!2026`
